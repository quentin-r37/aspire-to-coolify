/**
 * Coolify CLI command types
 */
export function formatCommand(cmd) {
    const parts = ['coolify', cmd.command, ...cmd.args];
    return parts.join(' \\\n  ');
}
export function formatOutput(output) {
    const lines = ['#!/bin/bash', '# Generated by aspire2coolify', ''];
    for (const cmd of output.commands) {
        if (cmd.comment) {
            lines.push(`# ${cmd.comment}`);
        }
        lines.push(formatCommand(cmd));
        lines.push('');
    }
    return lines.join('\n');
}
//# sourceMappingURL=coolify.js.map